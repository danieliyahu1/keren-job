{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "-y", "@playwright/mcp@latest", "--browser", "chromium", "--headless", "--user-data-dir", "/home/opencode/.playwright-mcp", "--output-dir", "/home/opencode/.playwright-mcp-output"],
      "enabled": true
    }
  }
}
